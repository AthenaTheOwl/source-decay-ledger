# Streamlit Community Cloud reads this file. The app (streamlit_app.py) reads the
# committed data/scores/*.jsonl + data/sources.yaml directly, so it only needs
# streamlit + pyyaml. The CLI/package under src/source_decay_ledger/ has its own
# deps in pyproject.toml.
streamlit>=1.40
pyyaml>=6.0
